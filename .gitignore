node_modules/
.next/
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# lib/api-types.gen.ts は意図的にコミットする:
# typecheck / docker build が import しており、生成ステップを持たない
# フレッシュ checkout でもビルドが通る必要がある。再生成は npm run generate:types。
tsconfig.tsbuildinfo

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/e2e/.auth/

# coverage
/coverage/
